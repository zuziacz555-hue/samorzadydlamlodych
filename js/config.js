/**
 * Global Configuration for Admin Access
 * This file updates automatically when you run the setup in Admin Mode.
 */
window.SITE_CONFIG = {
    auth: null // Will contain { cipher, iv, salt } after setup
};
